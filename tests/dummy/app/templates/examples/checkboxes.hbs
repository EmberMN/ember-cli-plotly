<ul>
{{#each dataSets as |dataSet|}}
  <li>{{input type="checkbox" name=dataSet.name checked=dataSet.isPassedToPlotly}}
    <label for={{dataSet.name}}>{{dataSet.name}}</label>
  </li>
{{/each}}
</ul>
{{plot-ly
  chartData=chartData
  chartLayout=chartLayout
  chartOptions=chartOptions
}}