<h3>List of examples</h3>
<ul>
  {{#each this.model as |example|}}
    <li>
      {{!--
      example.id = {{example.id}}<br>
      example.name = {{example.name}}<br>
      --}}
      {{link-to example.name (concat "examples." example.id)}}
    </li>
  {{/each}}
</ul>
